해시 테이블 

언제 어느상황에서 사용하는지 아는 것 이 중요

Array list 구현 => python의 딕셔너리 자료구조 

Array list + linked list based


충돌이 일어남 충돌 해결 방법 (면접 때 중요)

해시테이블의 정의

효율적 탐색, 빠른 탐색을 위한 자료 구죠이다.

key-value

해시테이블의 저장 삭제 검색의 시간복잡도가 O(1) 이다.

Direct-addreess Table

key값이 인덱스가 되는 


리턴값의 범위가 정해져 있음 

예시) 0~9까지 중 하나를 리턴

문자열 파라미터도 가능하다

해시 테이블의 문제 1

리턴값이 중복되면 저장할 수 없게 된다.

해결책 

open address
충돌시 지정한 양만큼(다음) 옆으로 이동 저장할 수 있을 때 까지

해시 function이 이미 python에서 구현되어 있다.
충돌이 일어날 확률이 적다.

hash function이 빅오 1 으로

Dictionary자료구조가 해시 테이블
 